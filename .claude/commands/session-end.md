Wrap up this working session:

1. Run `pytest tests/ -q`; fix anything that broke before proceeding.
2. Update CLAUDE.md: record any decisions made, bugs found/fixed, and open questions from this session (keep the "do not re-litigate" and "Open decisions" sections current).
3. Commit all changes with a clear, descriptive message and push to the current branch (`git push -u origin <branch>`).
4. Summarize in one short paragraph: what changed, what's tested, what's still open.
